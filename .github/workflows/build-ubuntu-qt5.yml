stages:
  - build

build-docker-image:
  stage: build
  image: docker:27
  services:
    - docker:29.7.0-dind
  variables:
    DOCKER_HOST: tcp://docker:2375
    DOCKER_TLS_CERTDIR: ""
  script:
    - docker version
    - docker build -f qt5/Dockerfile .
